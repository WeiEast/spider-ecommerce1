<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.datatrees.rawdatacentral.dao.OperatorExtractResultDAO">
    <insert id="insert" parameterType="com.datatrees.rawdatacentral.domain.model.OperatorExtractResult">
        <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Integer">
            SELECT LAST_INSERT_ID()
        </selectKey>
        insert into t_operator_extract_result (Id, TaskLogId, Duration,
        WebsiteId, OperatorId, Status,
        Remark, Url, UniqueSign,UniqueMd5,
        ResultType, StoragePath, CreatedAt)
        values (#{id}, #{taskId}, #{duration},
        #{websiteId}, #{operatorId}, #{status},
        #{remark}, #{url}, #{uniqueSign},#{uniqueMd5},
        #{resultType}, #{storagePath}, now())
    </insert>
</mapper>