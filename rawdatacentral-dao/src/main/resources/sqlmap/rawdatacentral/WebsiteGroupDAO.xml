<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.datatrees.rawdatacentral.dao.WebsiteGroupDAO">

    <select id="queryMaxWeightWebsite" resultType="com.datatrees.spider.operator.domain.model.WebsiteGroup">
        select * from website_group where group_code = #{groupCode} order by weight desc limit 1;
    </select>

    <update id="updateEnable" >
        update website_group set enable = #{enable} where website_name = #{websiteName}
    </update>

    <select id="queryWebsiteNameList" resultType="java.lang.String">
        select g.website_name
        from website_group g
        inner join website_operator o on g.website_name = o.website_name
        where
        1=1
        <if test="enable != null and enable != ''">
            and g.enable = #{enable}
        </if>
        <if test="operatorType != null and operatorType != ''">
            and o.operator_type = #{operatorType}
        </if>
        <if test="groupCode != null and groupCode != ''">
            and g.group_code = #{groupCode}
        </if>
    </select>
    <!--
    这个sql很重要
    select a.* from website_group a right join  (select group_code,max(weight) as weight from website_group group by group_code) b on a.group_code = b.group_code and a.weight = b.weight ;
    -->

</mapper>